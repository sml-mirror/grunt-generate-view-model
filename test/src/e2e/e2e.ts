"use strict";

import * as fs from "fs";
import {} from "mocha";
import {createViewModelsFunction} from "../../../src/index";
import {Options} from "../../../src/tasks/model/options";

import {expect} from "chai";


describe("E2E Tests", () => {

      it("Hero", (done) => {
          var options = new Options();
          //createFunc()
          var files = [];
          var file = {
            src:  ["test//src/model/hero/hero.ts"],
            dest: "test/dist/src/_autogenerated_viewmodels/heroes/common.ts",
            orig:
             { expand: true,
               cwd: "test/src/model",
               src: ["**/*.ts"],
               dest: "test/dist/src/_autogenerated_viewmodels" }
          };
          files.push(file);
          options.files = files;
          options.isOneFile = false;
          createViewModelsFunction(options);
          done();
      });

});
